<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>13</LangVersion>
    <Nullable>enable</Nullable>
    <AssemblyName>Plate.SCG.DI.ConstructorInjection</AssemblyName>
    <RootNamespace>Plate.SCG.DI.ConstructorInjection</RootNamespace>
    <PackageId>GiantCroissant.Plate.SCG.DI.ConstructorInjection</PackageId>
    <Version>$(GITVERSION_MAJORMINORPATCH)</Version>
    <Authors>Lunar Horse</Authors>
    <Description>Source generator for dependency injection constructor injection</Description>
    <PackageTags>source-generator;roslyn;di;dependency-injection;constructor</PackageTags>
    <IsRoslynAnalyzer>true</IsRoslynAnalyzer>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
    <DevelopmentDependency>true</DevelopmentDependency>
    <IsRoslynComponent>true</IsRoslynComponent>
    <IncludeBuildOutput>false</IncludeBuildOutput>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="3.3.4" PrivateAssets="all" />
    <PackageReference Include="Microsoft.CodeAnalysis" Version="4.3.0" PrivateAssets="all" />
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.3.0" PrivateAssets="all" />
    <!-- Plate.SCG.Shared.Abstractions is not yet published; attributes come from DI.ConstructorInjection.Attributes project reference -->
  </ItemGroup>

  <PropertyGroup>
    <PackageType>Analyzer</PackageType>
  </PropertyGroup>

  <ItemGroup>
    <None Include="_._" Pack="true" PackagePath="lib/netstandard2.0" Visible="false" />
  </ItemGroup>

  <Target Name="IncludeAnalyzerDllInPackage" AfterTargets="Build">
    <ItemGroup>
      <None
        Include="$(OutputPath)\$(AssemblyName).dll"
        Pack="true"
        PackagePath="analyzers/dotnet/cs"
        Visible="false" />
    </ItemGroup>
  </Target>

</Project>
